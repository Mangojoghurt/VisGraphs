```@meta
CurrentModule = VisGraphs
```

# VisGraphs

Documentation for [VisGraphs](https://github.com/Mangojoghurt/VisGraphs.jl).

---

## Public API

```@autodocs
Modules = [VisGraphs]
Filter = t -> !startswith(string(t), "_")
```
---

## Internals

These functions are internal and intended for package development or debugging only.
They may change or be removed without warning.

```@autodocs
Modules = [VisGraphs]
Filter = t -> startswith(string(t), "_")
```

---

## Index

```@index
Filter = t -> !startswith(string(t), "_")
```